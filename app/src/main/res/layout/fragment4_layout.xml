<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:focusableInTouchMode="true"
    android:orientation="vertical">

    <RelativeLayout
        android:id="@+id/topBar"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:background="@color/colorOrange">

    </RelativeLayout>

    <com.mancj.materialsearchbar.MaterialSearchBar
        android:id="@+id/searchBar"
        style="@style/MaterialSearchBarLight"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="6dp"
        app:mt_backIconTint="@color/colorOrange"
        app:mt_clearIconTint="@color/colorOrange"
        app:mt_dividerColor="@color/colorOrange"
        app:mt_hint="Enter your keyword"
        app:mt_maxSuggestionsCount="3"
        app:mt_placeholder="Search..."
        app:mt_placeholderColor="@color/colorOrange"
        app:mt_searchIconTint="@color/colorOrange" />

    <ListView
        android:id="@+id/listView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/topBar"
        android:layout_margin="10dp"></ListView>

</RelativeLayout>